services:
  web:
    image: php:8.2-apache
    # pensée a changé le nom du container name
    container_name: php-apache-To-do-list
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/var/www/html
    ports:
      - "8000:80"
    networks:
      - app-network

networks:
  app-network: